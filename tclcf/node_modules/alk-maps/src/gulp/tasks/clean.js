var config = require('../gulp-config')
var gulp = require('gulp')

//clean up old build
gulp.task('clean', function clean(callback) {
    var del = require('del')
    return del([config.paths.dist.root, config.paths.dist.coverage, config.paths.dist.deploy], callback)
})
