module.exports = function() {
    var api = {
        createLonLat: function(lon, lat) {
            return { lon: lon, lat: lat }
        }
    }

    return {
        api: api,
        constructor: sinon.stub().returns(api)
    }
}
