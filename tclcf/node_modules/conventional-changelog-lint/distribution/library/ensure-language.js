'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _franc = require('franc');

var _franc2 = _interopRequireDefault(_franc);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = (input, allowed) => {
	const detected = _franc2.default.all(input).filter(lang => lang[1] >= 0.45).map(lang => lang[0]);

	// franc spits out ['und'] when unable to
	// guess any languages, let it through in this case
	const matches = detected[0] === 'und' || detected.indexOf(allowed) > -1;

	return {
		matches,
		detected
	};
};

module.exports = exports['default'];