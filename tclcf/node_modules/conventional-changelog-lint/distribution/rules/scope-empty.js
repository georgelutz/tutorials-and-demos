'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _ensureNotEmpty = require('../library/ensure-not-empty');

var _ensureNotEmpty2 = _interopRequireDefault(_ensureNotEmpty);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (parsed) {
	let when = arguments.length <= 1 || arguments[1] === undefined ? 'never' : arguments[1];

	const negated = when === 'always';
	const result = (0, _ensureNotEmpty2.default)(parsed.scope);
	return [negated ? !result : result, ['scope', negated ? 'must' : 'may not', 'be empty'].filter(Boolean).join(' ')];
};

module.exports = exports['default'];