'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _ensureEnum = require('../library/ensure-enum');

var _ensureEnum2 = _interopRequireDefault(_ensureEnum);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = (parsed, when, value) => {
	const negated = when === 'never';
	const result = (0, _ensureEnum2.default)(parsed.scope, value);
	return [negated ? !result : result, [`scope must`, negated ? `not` : null, `be one of [${ value.join(', ') }]`].filter(Boolean).join(' ')];
};

module.exports = exports['default'];