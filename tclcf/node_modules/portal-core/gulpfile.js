var gulp = require('gulp')
var shared = require('./src/gulp')
var config = shared.getConfig()

//fix for test globals -- only needed in portal-core
config.mochaConfig.require = './src/test/testGlobals.js'

shared.importTasks(gulp, config)
