module.exports = (gulp, config) => {
    const stream = require('webpack-stream')
    const webpackConfig = require('../webpack.prod.config.js')

    return gulp.src(config.bundleInput)
        .pipe(stream(webpackConfig))
        .pipe(gulp.dest(config.dist))
}
