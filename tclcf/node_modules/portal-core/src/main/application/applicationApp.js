let angular = require('angular')

angular.module('portal-core-application', ['portal-core'])

    //components
    .component('applicationEdit', require('./components/ApplicationEdit'))
    .component('applicationList', require('./components/ApplicationList'))

    //config
    .config(require('./config/applicationCustomActionConfig'))
    .config(require('./config/applicationNavigationConfig'))
    .config(require('./config/applicationRouteConfig'))

    //components
    .component('applicationCard', require('./components/ApplicationCard'))


    //run
    .run(require('./run/applicationRestangularConfig'))
