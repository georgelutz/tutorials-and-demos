//the string below will be replaced with version.<gitSha>.json during
//deployment, allowing us to use this constant as a reference to that
//sha at run time
module.exports = 'version.json'.match(/version\.(\w+)?\.?json/)[1] || ''
