module.exports = /* @ngInject */
    function(themeService) {
        return {
            restrict: 'A',
            link: function(scope, element) {
                let color = themeService.getColor()
                element
                    .css('background-color', `rgb(${color[0]}, ${color[1]}, ${color[2]})`)
                    .css('color', `rgb(255, 255, 255)`)
            }
        }
    }
