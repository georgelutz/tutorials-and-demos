var _ = require('lodash')

module.exports = /* @ngInject */ function() {
    var indexConfig = {
        titleKey: 'core.common.title'
    }

    this.$get = function() {
        return _.cloneDeep(indexConfig)
    }

    this.config = indexConfig
}
