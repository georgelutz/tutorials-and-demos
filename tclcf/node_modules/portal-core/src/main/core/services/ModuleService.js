module.exports = class ModuleService {
    constructor() {
        this.angular = require('angular')
    }
    
    isModuleLoaded(module) {
        try {
            this.angular.module(module)
            return true
        } catch (e) {
            return false
        }
    }
}
