require('../styles/permissionCard.scss')

module.exports = {
    template: require('../views/permissionCard.html'),
    bindings: {
        permission: '<'
    },
    controller: class PermissionCard {
        /* @ngInject */
        constructor($mdMedia) {
            this.$mdMedia = $mdMedia
        }
    }
}
