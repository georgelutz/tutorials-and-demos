require('./permissionCard.scss')

module.exports = {
    bindings: {
        permission: '<'
    },
    template: require('./permissionCard.html'),
    controller: class PermissionCard {
        /* @ngInject */
        constructor($mdMedia) {
            this.$mdMedia = $mdMedia
        }
    }
}
