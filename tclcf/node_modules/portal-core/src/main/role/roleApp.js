var angular = require('angular')

angular.module('portal-core-role', ['portal-core'])
    //components
    .component('roleCard', require('./components/RoleCard'))
    .component('roleEdit', require('./components/RoleEdit'))
    .component('roleList', require('./components/RoleList'))

    //config
    .config(require('./config/roleCustomActionConfig'))
    .config(require('./config/roleNavigationConfig'))
    .config(require('./config/roleRouteConfig'))

    //run
    .run(require('./run/roleRestangularConfig'))
