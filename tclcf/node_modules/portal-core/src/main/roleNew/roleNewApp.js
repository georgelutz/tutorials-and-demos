const angular = require('angular')

angular.module('portal-core-role', ['portal-core'])

    //components
    .component('roleCard', require('./card/RoleCard'))
    .component('roleDetails', require('./details/RoleDetails'))
    .component('roleEdit', require('./edit/RoleEdit'))
    .component('roleList', require('./list/RoleList'))

    //config
    .config(require('./config/roleCustomActionConfig'))
    .config(require('./config/roleGlobalSearchConfig'))
    .config(require('./config/roleNavigationConfig'))
    .config(require('./config/roleRouteConfig'))

    //run
    .run(require('./run/roleRestangularConfig'))
