var angular = require('angular')

angular.module('portal-core-status', ['portal-core'])

    //components
    .component('maintenance', require('./components/Maintenance'))
    .component('statusGraph', require('./components/StatusGraph'))
    .component('statusView', require('./components/StatusView'))

    //config
    .config(require('./config/statusRouteConfig'))
