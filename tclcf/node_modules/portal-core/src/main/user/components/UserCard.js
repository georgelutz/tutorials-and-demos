require('../styles/userCard.scss')

module.exports = {
    template: require('../views/userCard.html'),
    bindings: {
        currentUser: '<',
        user: '<'
    },
    controller: class UserCard {
        /* @ngInject */
        constructor($mdMedia) {
            this.$mdMedia = $mdMedia
        }
    }
}
