require('./userCard.scss')

module.exports = {
    template: require('./userCard.html'),
    bindings: {
        currentUser: '<',
        user: '<'
    },
    controller: class UserCard {
        /* @ngInject */
        constructor($mdMedia) {
            this.$mdMedia = $mdMedia
        }
    }
}
