module.exports = {
    template: require('./userDetailsContainer.html'),
    controller: class UserDetailsContainer {
        /* @ngInject */
        constructor(userTabConfig) {
            this.userTabConfig = userTabConfig
        }

        $onInit() {
            this.tabs = this.userTabConfig.detailsTabs
        }
    }
}

