const angular = require('angular')

angular.module('portal-core-user', ['portal-core'])

    //components
    .component('userEditContainer', require('../userNew/edit/container/UserEditContainer'))
    .component('userEditProfile', require('../userNew/edit/profile/UserEditProfile'))
    .component('userDetailsContainer', require('../userNew/details/container/UserDetailsContainer'))
    .component('userDetailsProfile', require('../userNew/details/profile/UserDetailsProfile'))
    .component('userCard', require('../userNew/card/UserCard'))
    .component('userList', require('../userNew/list/UserList'))

    //config
    .config(require('../userNew/config/userCustomActionConfig'))
    .config(require('../userNew/config/userRouteConfig'))
    .config(require('../user/config/userGlobalSearchConfig'))
    .config(require('../user/config/userNavigationConfig'))

    //run
    .run(require('../user/run/userRestangularConfig'))

    //providers
    .provider('userTabConfig', require('../user/providers/userTabConfigProvider'))
