describe('LoginRoot', () => {
    var component
    var ctrl
    var themeService

    beforeEach(() => {
        themeService = {}
        themeService.getLogin = sinon.stub().returns('abc')

        component = require('../../../main/login/components/LoginRoot')
        ctrl = new component.controller(themeService)
        ctrl.$onInit()
    })

    it('should initialize', () => {
        ctrl.loginImageUrl.should.equal('abc')
    })
})
