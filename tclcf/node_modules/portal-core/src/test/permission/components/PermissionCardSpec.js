describe('PermissionCard', () => {
    let $mdMedia
    let component
    let ctrl

    beforeEach(() => {
        $mdMedia = {media: true}

        component = require('../../../main/permission/components/PermissionCard')
        ctrl = new component.controller($mdMedia)
    })

    it('should expose some variables', () => {
        ctrl.$mdMedia.should.equal($mdMedia)
    })
})
