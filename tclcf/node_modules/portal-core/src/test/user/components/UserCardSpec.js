describe('UserCard', () => {
    var component
    var ctrl
    var $mdMedia

    beforeEach(() => {
        $mdMedia = {}

        component = require('../../../main/user/components/UserCard')
        ctrl = new component.controller($mdMedia)
    })

    it('should expose some auth variables', () => {
        ctrl.$mdMedia.should.equal($mdMedia)
    })
})
